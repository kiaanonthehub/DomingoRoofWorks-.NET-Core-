@model IEnumerable<DomingoRoofWorks.Models.JobType>

@{
    ViewData["Title"] = "Index";
}

<h1>Job-Type Page</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Job Type
            </th>
            <th>
               Rate
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JobType1)
            </td>
            <td>
                R @Html.DisplayFor(modelItem => item.Rate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.JobTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JobTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JobTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
